Put your photos in this folder.

- Files in here are served by the dev server/build at `/my-photos/<filename>`.
- The local homepage automatically loads everything in this folder (and subfolders).
- If you add/remove photos while `npm run dev` is running, restart it so the manifest regenerates.

To view the local-photo homepage:

- Open the site at `/#local`
